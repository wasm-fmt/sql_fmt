/**
 * WASM formatter for SQL.
 *
 * @example
 * ```ts
 * import { format } from "@fmt/sql-fmt";
 *
 * const input = "SELECT * FROM table";
 * const output = format(input, "file.sql");
 * ```
 *
 * @module
 */
